# Image Overlay Tool

Resizes and crops a photo to fit a PNG overlay template, then pastes the overlay on top, using Pillow.

## Usage

Place your own overlay template as `shirt.png` in this folder, then run:

```
$ python shirt.py before.jpg after.jpg
```

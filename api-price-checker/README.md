# API Price Checker

Fetches the live Bitcoin price via the CoinCap REST API and converts a user-supplied amount into USD.

## Setup

```
export COINCAP_API_KEY="your-key-here"
```

## Usage

```
$ python bitcoin.py 2.5
$2.5 Bitcoin = $162,500.0000
```
